{
  "name": "pyx-log",
  "version": "1.0.0",
  "description": "Simple and pretty logging",
  "main": "index.js",
  "scripts": {
    "test": "mocha -b",
    "analyze-coverage": "istanbul cover _mocha ./index.js -- -u exports -R spec test",
    "check-coverage": "istanbul check-coverage --statements 100 --branches 100 --functions 100 --lines 100",
    "coverage": "npm run analyze-coverage && npm run check-coverage",
    "cover": "npm run coverage; http-server ./coverage/lcov-report/",
    "dev-docs": "npm run compile-docs && http-server api/",
    "compile-docs": "./node_modules/.bin/jsdoc -t node_modules/jaguarjs-jsdoc/ -c jsdoc-config.json -a all -d api index.js"
  },
  "author": "Alan Tirado <alan@pyxidis.io>",
  "license": "MIT",
  "dependencies": {
    "chalk": "^1.1.3"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "jaguarjs-jsdoc": "^1.0.1",
    "jsdoc": "^3.4.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/boxman0617/pyx-log.git"
  }
}
